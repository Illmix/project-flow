type Employee {
  publicId: String!
  Name: String!
  Email: String!
  Position: String
  created_at: String!
}

extend type Query {
  getEmployees: [Employee]
  getEmployee(publicId: String!): Employee
  me: Employee
}